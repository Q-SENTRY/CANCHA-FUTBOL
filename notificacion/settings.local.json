{
  "permissions": {
    "allow": [
      "Bash(node tests/integration.js)",
      "Bash(node server.js)",
      "PowerShell($proc = Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue | Select-Object -First 1; if \\($proc\\) { Stop-Process -Id $proc.OwningProcess -Force; Write-Host \"Servidor detenido\" } else { Write-Host \"Sin proceso en 3000\" })",
      "Bash(curl -s http://localhost:3000/api/notificaciones)"
    ]
  }
}
